<p id="notice"><%= notice %></p>

<% @songs.each do |song| %>
  <p><%= song.title %></p>
  <p><%= song.genre %></p>
  <p><%= song.rating %></p>

<% end %>


<%= link_to 'Edit', edit_song_path(@song) %> |
<%= link_to 'Back', songs_path %>
