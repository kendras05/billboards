<h1>New Artist</h1>

<%= form_for @artist do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>


  <%= f.submit %>
<% end %>


<%= render 'form', artist: @artist %>

<%= link_to 'Back', artists_path %>
